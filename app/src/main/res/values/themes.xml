<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlanTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:colorBackground">@color/screenBackground</item>
    </style>

    <style name="Theme.PlanTask" parent="Base.Theme.PlanTask" />
    
    <style name="Theme.PlanTask.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#1A73E8</item>
        <item name="android:windowSplashScreenAnimatedIcon" >@drawable/plan_task_logo</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.PlanTask</item>
    </style>
</resources>